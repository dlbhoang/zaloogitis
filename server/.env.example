PORT=4000
# Comma-separated list. Local dev also allows any localhost/127.0.0.1 port.
CLIENT_ORIGIN=http://localhost:5173,http://localhost:5174,http://localhost:5175

MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=
MYSQL_DATABASE=zalologistic

JWT_SECRET=change-me
JWT_EXPIRES_IN=7d

# Optional: if set, chatbot will call OpenAI. Otherwise it will reply locally.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini

